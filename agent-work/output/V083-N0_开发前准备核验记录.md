# V083-N0 开发前准备核验记录

记录时间：2026-08-07

## 已完成

- 从 `76e4788627bef621c500a3f82c5c63f6b21dcbed` 建立独立工作树 `case-board-v0.8.3-dev`；
- 建立分支 `fix/v0.8.3-data-safety`；
- 修订 v0.8.3 计划，冻结顺序为 `N0 → M1 → S1 → F1 → RC`；
- 将元典定时后台刷新和账户周期固化移出 v0.8.3；
- 建立 `V083-N0_开发前准备任务包.md`；
- 记录当前迁移 47—52 的 SHA-384；
- 使用 `pnpm install --frozen-lockfile` 安装锁定依赖，未修改锁文件；
- 核验 Node、pnpm、Rust/Cargo 实际版本和工作树 Cargo metadata。

## 基线验证

| 项目 | 结果 |
|---|---|
| `pnpm install --frozen-lockfile` | 通过，385 个包全部复用本地 store，lockfile 策略检查通过 |
| `pnpm test:logic` | 通过，119/119，失败 0、跳过 0 |
| `pnpm build` | 通过；存在既有的 chunk 大于 500 kB 警告，无构建错误 |
| `cargo metadata --no-deps --format-version 1` | 通过，workspace 和 target 路径正确 |
| `cargo check --lib -j 1` | 通过，冷缓存完整检查耗时 14 分 10 秒 |
| `cargo clippy --lib -j 1 -- -D warnings` | 通过，耗时 1 分 34 秒，警告按错误处理 |
| Windows Rust 清单主测试 | 通过，263 passed、0 failed、3 ignored |
| 设备同步契约测试 | 通过，18 passed、0 failed |
| Windows Rust 清单汇总 | 通过，3 个测试可执行文件全部成功 |

Rust 冷缓存预热曾分别在 5 分钟和 2 分钟短窗口内未完成；核验进程与 target 修改时间确认编译持续推进且无死锁后，使用 15 分钟窗口续跑并最终通过。该过程不记为代码失败，后续首次冷构建应预留足够时间。

## 安全核验

- 正式数据库：未读取、未修改；
- NAS 同步事件、manifest、同步组和成员密钥：未读取、未修改；
- 飞书 Base、Token 和业务记录：未调用、未修改；
- 产品源码、迁移文件、版本号和发布配置：未修改；
- 原 v0.8.2 工作树既有未跟踪证据：保留未清理；
- 两份修订计划 SHA-256 一致：`d12293097a143ced34c4c6e3e8051cad8742f1dc4d595db19d0bec8c07eed27a`。

## 待进入 N0 后完成

1. 建立迁移谱系失败夹具；
2. 建立循环外键及 501+ 实体分包失败夹具；
3. 从笔记本数据库只读副本取得历史 checksum，未取得前不建立兼容白名单；
4. 冻结 schema sentinel 和稳定错误码完整契约；
5. N0 结果经用户验收后才进入 M1 产品实现。
