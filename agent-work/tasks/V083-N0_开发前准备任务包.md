# V083-N0｜事实复现与契约冻结任务包

状态：`ready_for_execution`  
创建时间：2026-08-07  
工作树：`D:\CodexWorkspace\008案件看板应用\case-board-v0.8.3-dev`  
分支：`fix/v0.8.3-data-safety`  
基线：`76e4788627bef621c500a3f82c5c63f6b21dcbed`

## 一、任务目标

在不修改产品行为、不接触正式数据库和正式同步组的前提下，建立 v0.8.3 两个 P0 缺陷的确定性失败夹具，冻结迁移谱系分类、稳定错误码、同步依赖规则和后续实施边界。

## 二、执行前必读

1. `D:\CodexWorkspace\工作规则.md`
2. `D:\CodexWorkspace\踩坑日志.md`
3. `D:\CodexWorkspace\008案件看板应用\008_案件看板应用_日志.md`
4. `..\output\V083-20260803_下一轮待开发计划.md`
5. `D:\CodexWorkspace\008案件看板应用\笔记本v0.8.2闪退诊断与后续任务交接_20260803.md`
6. `D:\CodexWorkspace\008案件看板应用\case-board-v0.8.2-dev\agent-work\output\V082-BUG-20260803_设备同步基线外键隔离.md`
7. `D:\CodexWorkspace\008案件看板应用\case-board-v0.8.2-dev\agent-work\output\V082-BUG-20260803_飞书孤立绑定字段处理失败.md`

## 三、已核验基线

- 新工作树已建立且创建时干净；
- `HEAD` 与当时的 `origin/main` 均为 `76e4788`；
- `package.json` 版本为 `0.8.2`；
- 迁移范围为 1—62；
- 当前迁移逻辑先执行 `reconcile_migration_checksums()`，随后使用 `set_ignore_missing(true)`；
- 设备同步导出单事件上限为 500，导入按事件中操作顺序逐项执行；
- `device_sync_quarantine` 当前只有新增记录，没有活动/解决状态和重试计数；
- 正式台式机和笔记本同步截至 2026-08-03 已由用户暂停；开始任何现场动作前必须重新只读核验，不能把历史状态当成当前状态。

## 四、当前迁移 47—52 SHA-384

以下是当前发布谱系文件哈希，不等于允许兼容的历史 checksum 白名单。

| 版本 | 文件 | SHA-384 |
|---|---|---|
| 47 | `0047_case_legal_domain.sql` | `7c859e982563e09ec8400d4475985f5c5c682a97eeea367b8db4a89fc4aecafdd03424bd22607d7316e1b33807c0b465` |
| 48 | `0048_criminal_defense_workspace.sql` | `560f92033b9f5cf14af743d1f8c63505141c02f379b7bfc0c51157d8ed5ba1353e04aed4572c89efbdbd5df169e5b513` |
| 49 | `0049_feishu_case_management_sync.sql` | `5391dee7120f0715473530f80c0d3c336778bd29cde4c51cf7f367e4c3a5c85cc1a66483103e84850783eeee2641497c` |
| 50 | `0050_feishu_sync_field_previews.sql` | `2104cb8aa3e5523816994504876a45b23df166267e5cbfa6e60191c967f405e07339aee45805d0a126a9402415dd9d88` |
| 51 | `0051_feishu_manual_binding.sql` | `480cba0c5b59775e7ca65ff30be83e7c57b53f221085229bb75173f7a5f0c687b6297aae7b7e1f3e16a047eb4458b144` |
| 52 | `0052_feishu_inbound_entities.sql` | `0b2515cf4e1d8bfb8e7e090cf8127011ede92ba70b246773b871d0c064d17c6b9ef4be505fb186874a67eb254082fe16` |

笔记本历史数据库中的旧 checksum 尚未在本工作树取得。必须从 SQLite 只读副本读取版本、description、success 和 checksum；只输出迁移元数据，不输出案件内容。取得前不得编造白名单。

## 五、N0 工作项

### N0-A｜迁移谱系夹具

创建测试辅助代码或测试资源，构造：

1. 全新库；
2. 当前 62 迁移正常库；
3. 已知 checksum 差异但 schema 完全匹配；
4. 版本 49 success=1 但无 `feishu_sync_inbox`；
5. 同编号不同 SQL；
6. 当前二进制未知的已应用版本；
7. success=0 的失败迁移。

断言未修复实现会暴露当前风险；夹具不得依赖正式数据库文件。

### N0-B｜schema sentinel 清单

至少冻结：

- 49：`feishu_sync_inbox`、`feishu_sync_links` 及关键列/外键；
- 51：`feishu_sync_inbox.auto_bind_suppressed`；
- 58：设备同步核心表、关键外键；
- 59：安全加固新增列/索引；
- 60—62：领域隔离及飞书受控双向相关表、列、触发器。

只检查结构，不查询业务字段值。

### N0-C｜设备同步失败夹具

使用纯合成 ID 和文本，覆盖：

1. case 先于 contact，case 带非空 `judge_id`；
2. contact 反向引用 case；
3. 失败后事务无部分写入；
4. 同一包重复调度产生重复隔离；
5. 501 条以上实体使引用两端跨默认分包边界；
6. import 返回隔离但 audit 仍为 `succeeded` 的现状。

### N0-D｜稳定错误码契约

为计划中 8 个错误码记录：触发条件、是否可重试、是否自动暂停、用户文案、日志字段和禁止展示的数据。不得依赖中文错误文本进行程序分类。

## 六、允许修改范围

N0 阶段只允许修改或新增：

- `src-tauri/src/db/` 下的测试模块或纯测试辅助文件；
- `src-tauri/src/device_sync/` 下的测试模块或纯测试辅助文件；
- `scripts/windows-upgrade-validation/` 下的隔离数据库夹具；
- `agent-work/tasks/`、`agent-work/output/` 下的任务、契约和测试报告；
- 项目主日志中的阶段记录。

N0 不得修改生产路径行为、正式数据库、NAS 文件、同步状态、飞书 Base、凭据、版本号或发布配置。

## 七、验证命令基线

按锁文件和项目脚本执行，所有构建串行：

```powershell
$env:PATH = 'C:\Users\William Feng\.cargo\bin;' + $env:PATH
$env:CARGO_INCREMENTAL = '0'
pnpm test:logic
pnpm build
cargo check --lib -j 1
cargo clippy --lib -j 1 -- -D warnings
powershell.exe -ExecutionPolicy Bypass -File .\scripts\run-windows-rust-tests.ps1
```

若缺少依赖，只能使用 `pnpm install --frozen-lockfile`；不得运行 npm install。若 Cargo 出现增量缓存占用，保持 `CARGO_INCREMENTAL=0` 重验，不删除整个 target。

## 八、提交与退出门禁

建议提交拆分：

1. `test: reproduce migration lineage incompatibility`
2. `test: reproduce cyclic device sync baseline failure`
3. `docs: freeze v0.8.3 compatibility contracts`

N0 完成时必须报告：

- 失败夹具名称及未修复时的预期失败；
- 当前迁移 checksum 与已知旧 checksum 的来源；
- schema sentinel 清单；
- 稳定错误码契约；
- 实际运行的命令、通过/失败/跳过数量；
- Git 状态和提交号；
- 明确声明未修改正式数据库、NAS、飞书和凭据。

只有 N0 证据经用户验收后，才能进入 V083-M1 产品实现。
