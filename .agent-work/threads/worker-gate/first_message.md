# worker-gate 首条消息

你负责 `V083-N0-GATE`。进入 `D:\CodexWorkspace\008案件看板应用\case-board-v0.8.3-dev`，先读取本线程 `task.md` 及其中全部必读文件。开始时运行 workflow `start`，只做只读审计并写指定报告，完成后运行 workflow `submit`。不得改产品或测试源码、不得自行验收、不提交 Git。当前为共享工作树，忽略并保留其他 Agent 的并行修改。
