# worker-sync 首条消息

你负责 `V083-N0-SYNC`。进入 `D:\CodexWorkspace\008案件看板应用\case-board-v0.8.3-dev`，先读取本线程 `task.md` 及其中全部必读文件。开始时运行 workflow `start`，完成后写入指定报告并运行 workflow `submit`。只写允许范围，不改生产逻辑、不接触正式数据库/NAS、不自行验收、不提交 Git。当前为共享工作树，保留其他 Agent 修改。Cargo 全量验证由主控串行执行。
