# 10 第一轮分派计划（V083-N0）

## 本轮目标

建立三组互不重叠的失败证据和契约，为 M1/S1 实现冻结输入。

## 并行任务

1. `V083-N0-MIG`：迁移谱系夹具、sentinel 与错误码契约；
2. `V083-N0-SYNC`：循环外键、501+ 分包、隔离状态夹具；
3. `V083-N0-GATE`：独立审计发布门禁、测试缺口和跨任务冲突。

## 写入隔离

- MIG 只写 db 测试/测试辅助与自己的交付物；
- SYNC 只写 device_sync 测试/测试辅助与自己的交付物；
- GATE 只写 `.agent-work/threads/worker-gate` 及指定报告，不改产品源码。
