# V084-N0 分派计划

## 上游任务

| task_id | 目标 | 允许修改 |
| --- | --- | --- |
| V084-N0-UPDATER | 审计更新生命周期、持久化状态、发布原子性及 ASCII 资产，形成实现契约 | 仅对应线程状态与 `.agent-work/output/V084-N0-UPDATER.md` |
| V084-N0-TODO | 审计现有待办、案件进展和页面结构，冻结本地模型与复制防重契约 | 仅对应线程状态与 `.agent-work/output/V084-N0-TODO.md` |
| V084-N0-FEISHU | 审计飞书现有受控同步能力，冻结“收件箱”双向同步、冲突和删除边界 | 仅对应线程状态与 `.agent-work/output/V084-N0-FEISHU.md` |

三任务可并行，只读源码并写各自报告。不得修改产品源码、迁移、版本号、workflow、发布清单或正式外部状态。

## N0 合并输出

主控在三份报告均提交后形成 `.agent-work/output/V084-N0-CONTRACT.md`，冻结：

- 数据表/字段/迁移策略；
- 更新状态机及一次性凭据位置；
- 飞书字段映射、同步方向、冲突/删除/去重规则；
- 案件进展复制事务和稳定错误码；
- 后续任务依赖、允许文件范围和验收命令。
