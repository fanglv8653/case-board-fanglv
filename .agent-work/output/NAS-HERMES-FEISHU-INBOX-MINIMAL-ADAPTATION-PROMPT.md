# 发给 NAS-Hermes 的提示词

下面内容可直接发送给赫敏：

```text
请对现有“飞书收件箱”接入做一次最小适配，直接执行，不重建表，不迁移历史数据，也不要重构无关功能。

固定目标：
- Base Token：TB7mbdT29aqO6gsn7LecjEAbnTb
- Table ID：tblVpYnIMz6DZYrm（📥收件箱）
- View ID：vewnPR6sL7（收件箱）

表内仍有22个字段和17条历史记录。当前视图只显示这14个业务字段：事项、事项编号、原始内容、类型、状态、优先级、下一步动作、截止时间、提醒时间、关联案件、标签、完成时间、删除请求时间、删除原因。

以下字段只是被隐藏，没有删除：信息强度、提醒次数、最后提醒时间、关联项目、外部归档路径、来源消息ID、来源时间、更新时间。你仍可按字段名读写其中必要字段，但不要把它们重新设为可见，也不要新增“同步版本、哈希、删除状态”等技术列。

请按以下规则调整现有收件箱工具或技能：

1. 保留现有事项编号P0000、P0001……作为稳定业务编号，不改写旧编号。新建时沿用P加4位数字；只查询当前最大编号，不得为取号读取整表正文。写入前再精确检查拟用编号是否已存在。
2. 每条来源消息先按“来源消息ID”精确查重。已存在时只更新确实变化的字段；不存在时才新建。不要按相似标题自动合并。
3. 新建记录只写必要字段：事项、事项编号、原始内容、类型、状态、优先级、信息强度、来源消息ID、来源时间、更新时间、提醒次数。默认状态为“收件箱”，提醒次数为0。没有值的可选字段保持空，不补当前时间或猜测内容。
4. 类型只能是：想法、待办、提醒、资料、备忘。状态只能是：收件箱、进行中、等待、完成、删除待确认、已删除。优先级只能是：高、中、低、未判断。信息强度只能是：明确、倾向、可能、待确认。不要自行创建新选项。
5. “截止时间”和“提醒时间”分开处理。用户表达最后期限时写截止时间；明确要求届时提醒时才写提醒时间。时间使用Asia/Shanghai。无法确定具体时间就留空或向用户问一个短问题，不得猜测。
6. “关联案件”只在用户明确说出案件时填写原文。案件名称相似、简称不清或存在多个候选时留空并询问，不做模糊绑定。“关联项目”和“外部归档路径”停止写入。
7. 标记完成时写状态=完成、完成时间和更新时间。删除采用软删除：先写状态=删除待确认；收到用户明确确认后再写状态=已删除、删除请求时间、删除原因和更新时间。禁止物理删除记录。
8. 真正发送提醒后，才更新最后提醒时间并将提醒次数加1；没有发送成功就不要改这两个字段。

降低DeepSeek token消耗：
- 启动或表结构变化时读取一次字段结构并缓存；正常处理不要反复读取schema。
- 查询时只投影本次需要的字段，不读取整表、原始长内容或全部历史记录。
- 一次消息最多执行“精确查重+一次最小写入”；只有编号冲突、案件歧义或写入结果不确定时才追加查询。
- 不输出分析过程。成功后只回复“已记录：Pxxxx｜事项”；需要补充时只问一个最短问题。
- schema缺字段、字段类型或选项不符时停止写入，简短报告差异；不要自动建字段、改字段、删字段或重试写入。

完成适配后做一次只读验收：确认Base、Table、View ID正确，22个字段仍存在，原有17条历史记录均保留，默认视图为上述14列；期间正常新增的记录不算异常。不要创建测试记录。最后只向我报告：改了哪些配置或文件、备份位置、验收结果；不要输出密钥、token、整表内容或长日志。
```
