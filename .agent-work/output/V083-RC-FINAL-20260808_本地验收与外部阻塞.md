# V083-RC-FINAL｜0.8.3 本地验收与外部阻塞

- 本地 RC 状态：`accepted`
- 正式发布状态：`blocked_external`
- 分支：`fix/v0.8.3-data-safety`
- 本地 RC 提交：`b91e6913c5b61dfd57c46c1bf76c97f145063429`
- 独立复核：P0=0、P1=0；两项 P2 已由主控收口

## 已完成

1. 按 `N0 → M1 → S1 → F1 → RC` 完成迁移谱系、设备同步、飞书孤立绑定及本地发布门禁开发与分阶段验收。
2. 五处版本源已统一为 0.8.3；`release/latest.json` 继续指向已公开的 0.8.2。
3. 真实 pre-0063 临时库通过生产 `init_pool` 升级至 0063并幂等重开，脱敏标记保留，`quick_check=ok`、FK 空。
4. 两个临时文件数据库与全新临时 mounted folder 完成 A→B→A、无变化幂等、坏包隔离、显式恢复重放及最终收敛。
5. Node 44 文件 123 项通过；Windows Rust 396 项通过、4 项按设计 ignored；Cargo check、Clippy `-D warnings`、TypeScript、Vite、source gate、release-resume、Python 升级契约和截图自测通过。
6. `pnpm tauri build --no-bundle` 成功生成本地 release EXE：FileVersion/ProductVersion 0.8.3，19,440,128 bytes，SHA-256 `277F1B151567AC5FB941E0CC28D7D19A389B022659216686B8A00985B08FCC61`，Authenticode 如实为 `NotSigned`。

保留的旧定向迁移失败日志属于重新编译前的中间证据，已经由 manifest 嵌入后的定向复跑和 Windows Rust 全量 396 项通过覆盖。六个仅显示为修改的设备同步文件经索引/工作树内容哈希逐一确认完全相同，已从提交范围排除。

## 未授权或缺少外部输入，不能代替完成

1. 来源可追溯的历史 0.8.2 checksum、发布迁移 SQL 哈希、完整 sentinel 与在线一致性只读副本；未取得前继续对未知谱系写前失败关闭。
2. 受控 CI 中的 updater signing secrets、0.8.3 NSIS setup 与同名 `.sig`，以及最终资产的 minisign/SHA-256 核验。
3. commit push、`v0.8.3-fanglv` tag、GitHub Actions、Release 和 `release/latest.json` 快进发布授权。
4. 指定 0.8.2 物理测试端的备份、在线下载/验签/安装/重启升级验收。
5. 两台可回滚 Windows 物理设备、全新隔离同步目录和新测试组的 A→NAS→B→NAS→A 验收。
6. release EXE 启动冒烟所需的可丢弃 Windows VM/全新无正式凭据用户，或仅测试可用的临时凭据后端。

## 发布纪律

- 在上述外部条件齐备并另行明确授权前，不 push、不 tag、不创建 Release、不更新 `release/latest.json`，不访问正式数据库、当前失败同步组、正式 NAS 或正式飞书资源。
- 本地 RC 接受仅证明当前提交具备进入受控正式发布验证的条件，不等于 0.8.3 已公开发布。
