# V083-FORMAL-TOOLING-R1 验收标准

- P0=0、P1=0。
- 默认调用不写正式数据、不安装、不切换目录。
- source trio 前后不变与 main-only 目标为硬断言，不是日志提示。
- 快照比较能发现行数不变但内容变化。
- 不存在删除正式 sidecar、修改 `_sqlx_migrations`、自动恢复同步或自动发布路径。
- 合成测试全部通过；独立只读复核通过后，才允许主控使用工具建立正式备份。
